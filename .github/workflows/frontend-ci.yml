name: Frontend CI
on:
  pull_request:
    paths: ['frontend/**', 'common/**']
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cd frontend && npm ci
      - run: cd frontend && npx tsc # Type-check